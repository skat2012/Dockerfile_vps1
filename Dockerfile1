FROM ubuntu:18.04
ADD requirements.txt /tmp/
ADD app.py /opt/webapp/
RUN apt update
RUN DEBIAN_FRONTEND=noninteractive apt-get install -y -q python-all python-pip -qr requirements.txt
EXPOSE 5000
CMD ["python", "/opt/webapp/app.py"]